
export const EducatorsDirectory = ({ members, isLoading, searchQuery, hasNextPage, fetchNextPage, isFetchingNextPage }) => {
  return (
    <div className="text-center text-gray-400">
      <p>Educators directory is not available in this preview</p>
    </div>
  );
};
